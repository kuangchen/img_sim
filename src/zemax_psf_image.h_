#ifndef _ZEMAX_PSF_IMAGE_H
#define _ZEMAX_PSF_IMAGE_H

#include <string>
#include "boost/multi_array.hpp"
#include <include/grid_2d.h>


using namespace std;

namespace img_sim {
  
  class zemax_psf_image : public grid_2d<double, double> {
  public:
    typedef grid_2d<double, double>::geometry geometry;

  private:

  public:
    zemax_psf_image(const string &fname);
    static geometry parse_geometry(const string &fname);

  };

}

#endif
